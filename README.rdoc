How to run the application
==========================
1) Download the source code and extract it which I sent through mail
2) install the bundle run 'bundle install'
3) start the rails server run 'rails s'
4) go to your browser and access 'http://localhost:3000'